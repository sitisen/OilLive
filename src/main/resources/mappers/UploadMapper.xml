<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oillive.dao.UploadDao">
	
	<!-- 문의등록 -->
	<insert id="upLoadQ" parameterType="PhotoVO">
		INSERT INTO photo 
		VALUES (seq_photo.NEXTVAL, null, seq_q_board.CURRVAL, null, #{photoName}, #{photoReName}, #{photoPath})
	</insert>
	
	<!-- 상품등록 -->
	<insert id="upLoadG" parameterType="PhotoVO">
		INSERT INTO photo 
		VALUES (seq_photo.NEXTVAL, seq_goods.CURRVAL, null, null, #{photoName}, #{photoReName}, #{photoPath})
	</insert>
	
	<!-- 이벤트등록 -->
	<insert id="upLoadE" parameterType="PhotoVO">
		INSERT INTO photo 
		VALUES (seq_photo.NEXTVAL, null, null, seq_event.CURRVAL, #{photoName}, #{photoReName}, #{photoPath})
	</insert>
	
	<!-- 업로드된 파일 변경 -->
	<update id="upLoadU" parameterType="PhotoVO">
		 UPDATE photo
			SET PHOTO_NAME = #{photoName},
	     		PHOTO_RENAME = #{photoReName},
	     		PHOTO_PATH = #{photoPath}
	 	  WHERE PHOTO_CODE = #{photoCode}
	</update>

</mapper>